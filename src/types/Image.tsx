export interface Image {
    id: string;
    src: string;
    source: string;
    description?: string;
  }
  
  export interface FirestoreImage {
    id: string;
    src: string;
    source: string;
    description?: string;
  }
  